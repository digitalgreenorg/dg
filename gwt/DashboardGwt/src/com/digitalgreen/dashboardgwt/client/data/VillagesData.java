package com.digitalgreen.dashboardgwt.client.data;

import com.digitalgreen.dashboardgwt.client.common.RequestContext;

public class VillagesData extends BaseData {

	protected static String CREATE_TABLE = "CREATE TABLE IF NOT EXISTS `village` (id INTEGER PRIMARY KEY  NOT NULL ,VILLAGE_NAME VARCHAR(100)  NOT NULL ,block_id INT  NOT NULL DEFAULT 0,NO_OF_HOUSEHOLDS INT  NULL DEFAULT NULL,POPULATION INT  NULL DEFAULT NULL,ROAD_CONNECTIVITY VARCHAR(100)  NOT NULL ,CONTROL SMALLINT  NULL DEFAULT NULL,START_DATE DATE  NULL DEFAULT NULL, FOREIGN KEY(block_id) REFERENCES block(id)); " +
			"CREATE TABLE IF NOT EXISTS `monthly_cost_per_village` (id INTEGER PRIMARY KEY  NOT NULL ,village_id INT  NOT NULL DEFAULT 0,DATE DATE  NOT NULL ,LABOR_COST FLOAT(0,0)  NULL DEFAULT NULL,EQUIPMENT_COST FLOAT(0,0)  NULL DEFAULT NULL,TRANSPORTATION_COST FLOAT(0,0)  NULL DEFAULT NULL,MISCELLANEOUS_COST FLOAT(0,0)  NULL DEFAULT NULL,TOTAL_COST FLOAT(0,0)  NULL DEFAULT NULL,PARTNERS_COST FLOAT(0,0)  NULL DEFAULT NULL,DIGITALGREEN_COST FLOAT(0,0)  NULL DEFAULT NULL,COMMUNITY_COST FLOAT(0,0)  NULL DEFAULT NULL, FOREIGN KEY(village_id) REFERENCES village(id));";  
	
	public VillagesData(RequestContext requestContext) {
		super();
	}
}